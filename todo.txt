- sign-in/create account screen - add language/localization switch. When user switch, remeber for the rest of app, but keep possibility to change it in the settings as now.
- kiosk mode - rights as a keg session owner, but not showing user specific menu - nickname, profile, not in participants list. This mode will be turned on eg. on tablet which will stay next to keg to be available to record taps for every user
- brewery&beers DB, use for autocomplete
- sign-in via Google provider
- do not use Cloud functions - add to copilot-instructions
- sign in automatically - send data when filled
- when pour (or pour for someone) remember selected volume for next pour. Anyone who do the pour will override last used volume. 
- avoid using heredoc, it is causing terminal disconnection ("cat > /Users/ondrejhnyk/Documents/BeerEr/lib/l10n/app_cs.arb << 'ARBEOF' {  "@@locale"" with long text), use script (python) instead - write it into your copilot-instructtions.md. Make you sure you udnerstand it.
- keep in mind localization, when adding some new code - screens, check if all localization is ready
- keg chart (volume x time, rate x time)
- gmail login
- BAC - total consumption over all active sessions
- participant consumption - keg session + total active consumptions (like 5/15 - meaning 5 in current active keg session and 15 total over all active keg sessions). In completed  keg sessions show  only consumption related to that keg session.
- small resolution - when navigation buttons on screen, then last buttons are not visible or unable to scroll to view them, text in buttons is wrapped (consider using smaller font, disable wrap, statck buttons in rows if no space  to be next each other)
- currency selector during filling keg price, this currency will be used in all keg session related visualization (participants, bills), remove currency selector from settigs and remove from user model
